{
  "name": "marketdata-docusaurus-proxy",
  "private": true,
  "scripts": {
    "test": "vitest run --exclude '**/*.integration.test.js'",
    "test:integration": "vitest run .integration.test. --test-timeout=30000"
  },
  "devDependencies": {
    "vitest": "^3.1.0"
  }
}
